# Ts Template with Jest for Codingame

## Usage

link CodinGame Sync with "src/out.ts"

* `npm run test` to run Jest
* `npm run test:watch` to run jest --watch
* `npm run bundler` to output your code in a single Ts file "src/out.ts"

